import 'package:flutter/material.dart';

class SearchRepositoriesPage extends StatelessWidget {
  final String query;

  const SearchRepositoriesPage({
    super.key,
    required this.query,
  });

  @override
  Widget build(BuildContext context) => Container();
}
